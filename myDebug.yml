---
- hosts: 172.31.5.252
  become: yes
  tasks:
  - name: i want to execute direct linux command
    command: hostname -f
    register: myfirstresult

  - name: i want to debug my first task 
    debug: 
       msg: "{{myfirstresult}}"
